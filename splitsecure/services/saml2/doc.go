// Package saml2 implements the SplitSecure SAML2 identity-provider and
// service-provider Terraform resources. Mirrors the upstream service-
// per-package layout (e.g. terraform-provider-aws's
// internal/service/<svc>/) so each integration domain owns its own
// schema + build + read paths.
package saml2
